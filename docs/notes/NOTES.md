ARIS V3 UI is `frontend-next/` (Next.js). The older Vite app lives on the
`legacy-vite-frontend` branch and is not part of this tree.

Local UI: `cd frontend-next && npm install && npm run dev`
API: `uvicorn backend.main:app --port 8765`
