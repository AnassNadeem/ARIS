# Build-time API origin for the Vite UI (Cloudflare Pages injects this).
# Empty = same-origin /api (only works if Pages proxies to Heroku).
# For a split Pages + Heroku beta, set this in the Pages build environment:
#   VITE_API_BASE_URL=https://YOUR-APP.herokuapp.com
VITE_API_BASE_URL=
