# Streamlit Community Cloud reads this file, not pyproject.toml.
# Mirrors the runtime subset of [project].dependencies that the deployed app
# actually imports. FastF1 stays in — fetch_seasons/fetch_races never touch it
# at request time, but keeping the same import surface as local rules out a
# whole class of "works on my laptop" surprises.
streamlit>=1.40
pandas>=2.0
numpy>=1.26
sqlalchemy>=2.0
psycopg[binary]>=3.1
python-dotenv>=1.0
altair>=5.0
plotly>=5.24
scikit-learn>=1.4
xgboost>=2.0
pydantic>=2.0
httpx>=0.27
pyyaml>=6.0
faiss-cpu>=1.8
fastf1>=3.4
matplotlib>=3.8
signalrcore>=0.9
requests>=2.31
websocket-client>=1.8
